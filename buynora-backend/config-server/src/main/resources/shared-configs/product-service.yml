server:
  port: 8083

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/buynora_product
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
