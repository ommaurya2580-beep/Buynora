spring:
  datasource:
    url: jdbc:postgresql://db-test:5432/buynora_auth_test
  jpa:
    hibernate:
      ddl-auto: create-drop

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-test:8761/eureka/
